{
  "name": "remindr",
  "type": "module",
  "private": true,
  "scripts": {
        "bg:manifest": "node scripts/generate-background-manifest.mjs",
    "dev": "npm run bg:manifest && nuxt dev",
    "build": "npm run bg:manifest && nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "@nuxt/fonts": "^0.14.0",
    "@nuxtjs/tailwindcss": "^6.14.0",
    "nuxt": "^4.3.1",
    "vue": "^3.5.28",
    "vue-router": "^4.6.4"
  }
}
